--Deploy Actions
Deploy = {}

function Deploy:WriteDeploySettings
	--Create the Settings file for deployment

	--Pull options from server


	--set options unique to remote factory
end

function Deploy:TransmitFileBuffer(filebuffer,address)
	if filebuffer then
		
	end
end
function Deploy:RemoteFactoryProcessEEPROM(address)
	--Pass the Intermediate EEPR
end
function Deploy:RemoteFactorySetup(address)
	--Deploy process to set up a remote factory

	--Create the Settings file
end